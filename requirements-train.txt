# Training and pipeline dependencies — install locally when retraining models.
# Not needed by the Streamlit app.
#
# Usage:
#   pip install -r requirements-train.txt

-r requirements.txt

# Class imbalance handling (SMOTE)
imbalanced-learn

# Alternative model architectures evaluated during development
xgboost==3.0.5
torch==2.8.0
torchvision==0.23.0
keras-tcn

# Video processing and camera calibration
opencv-python==4.12.0.88

# Data exploration and fast transforms
matplotlib==3.10.6
polars==1.34.0

# YOLO-based object detection (planned future extension)
ultralytics==8.3.204

# Development and linting tools
ruff
pytest
